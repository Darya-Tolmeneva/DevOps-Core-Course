apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devops-info.fullname" . }}-test-connection"
  labels:
    {{- include "devops-info.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devops-info.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
